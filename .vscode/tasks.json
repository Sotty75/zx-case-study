{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build ASM file",
            "type": "shell",
            "command": "sjasmplus --sld=sld\\symbols.sld --fullpath \"${file}\"",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}